# Production / Render backend (no Vite proxy)
# VITE_API_URL=https://your-api.onrender.com

# Local dev — browser calls /api, Vite proxies to the Nest backend
VITE_API_URL=/api
VITE_BACKEND_URL=http://localhost:3006
